// just like ruby we can create a funtion without creating a class etc.
function sayHello(name) {
  console.log("Hello " + name);
}

sayHello("Cohort 16"); //don't actually need the semi colon, the compiler takes care of.
